#include <stdio.h>
#include <stdlib.h>

int main(){

    //Faça um algoritmo/programa que leia vários valores entre 0 e 9 e armazene-os em uma lista
    //até que o usuário digite um valor fora deste intervalo. Em seguida o programa deve informar
    //qual valor apareceu mais vezes nesta contagem e quantas vezes ele apareceu

    int vetor[ind], i, ind;

    while(vetor>0 && vetor<10){
        ind=0;
        printf("Insira valores entre 0 e 9: ");
        scanf(" %d", &vetor[ind]);
        ind++;
        if(vetor[ind]<0 || vetor[ind]>9){
            break;
        }
        //Não consegui ;-;
        if()

    }
    printf("O numero %d apareceu %d vezes", num)
}